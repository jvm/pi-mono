# Contributing

Run these checks before opening a pull request:

```bash
npm run check --workspace packages/pi-ollama
npm test --workspace packages/pi-ollama
npm run pack:dry-run --workspace packages/pi-ollama
```
